{
  "name": "mern-app-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "npm --prefix client install && npm --prefix backend install",
    "build": "npm --prefix client install && npm --prefix client run build",
    "start": "npm run build && node backend/server.js"
  },
  "engines": {
    "node": "18.x"
  }
}
